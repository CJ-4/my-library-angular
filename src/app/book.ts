export class Book {
    title: string;
    author: string;
    year: number;

    constructor(title: string, category: string, year: number) {
        this.title = title;
        this.author = category;
        this.year = year;
    }
}

